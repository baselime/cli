name: validate

on: push

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - uses: rome/setup-rome@v0.4
        with:
          version: cli/v11.0.0-nightly.aec33ef
      - run: rome ci .